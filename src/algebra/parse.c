#include "../../include/parse.h"
#include "../../include/equation_objects.h"

// 2+2*4x
int parse_expr(struct EquationObject *input, int length,
               struct ParseObject *buffer, int max_length) {
  int out_count = 0;
  
  struct ParseObject def_obj;
  struct ParseObject cur_obj;
  
  

  return out_count;
}
